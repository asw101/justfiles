#!/usr/bin/env bash
set -euo pipefail

session="${CODEX_TMUX_SESSION:-codex}"

if tmux has-session -t "=$session" 2>/dev/null; then
  exec tmux attach-session -t "=$session"
fi

exec tmux new-session -s "$session" /usr/local/bin/codex-auto "$@"
