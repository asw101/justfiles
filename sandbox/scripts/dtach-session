#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 2 ]; then
  echo "usage: dtach-session SOCKET COMMAND [ARG...]" >&2
  exit 2
fi

socket="$1"
shift

exec terminal-session dtach -A "$socket" -z -r winch "$@"
